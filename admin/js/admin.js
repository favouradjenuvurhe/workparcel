/* Workparcel admin UI behaviors. Currently a no-op placeholder — the
 * Settings tabs are implemented with pure CSS (radio inputs + labels)
 * so the admin screens keep working even if this script fails to load. */
